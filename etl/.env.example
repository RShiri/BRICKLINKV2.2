# PostgreSQL connection string for the Supabase database (session pooler or direct).
# Example: postgresql://postgres.<project-ref>:<password>@<region>.pooler.supabase.com:5432/postgres
DATABASE_URL=

# Optional: secret shared with the Next.js app to trigger ISR revalidation after imports.
REVALIDATE_URL=
REVALIDATE_SECRET=
